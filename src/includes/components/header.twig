<header class="header">
  <div class="container">
    <div class="flex items-center">
      <div class="mx-auto sm:mx-0 {% if logo == false %}lg:hidden{% endif %}">
        <a href="index.html" class="flex items-center font-semibold text-2xl text-gray-600" aria-label="Notices">
          <img src="{{ logoImage }}" class="flex-shrink-0 mr-2" alt="">
          <span class="">Notices</span>
        </a>
      </div>
      {% if step %}
        {% include "../blocks/steps.twig" %}

      {% endif %}
      {% if headerPhone == true %}
        <div class="hidden lg:block ml-auto">
          <a href="tel:0805086524">
            <img src=" assets/img/phone2.png" alt=" ">
          </a>
        </div>
      {% endif %}
    </div>
  </div>
</header>
